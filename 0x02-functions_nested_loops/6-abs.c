#include "main.h"

/**
*_abs - computes the absolute value of an integer
*@n: variable to be checked
*Return:n if n>=0,therwise -n.
*/

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
